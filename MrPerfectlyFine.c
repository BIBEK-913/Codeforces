#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    int t, n, i, j, time;
    char skill[2];
    scanf("%d", &t);
    for (i = 0; i < t; i++)
    {
        scanf("%d", &n);
    }
    return 0;
}